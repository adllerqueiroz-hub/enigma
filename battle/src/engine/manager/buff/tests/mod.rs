use sonettobuf::{BuffInfo, Fight, FightEntityInfo, FightTeam, HeroAttribute};

use super::*;
use crate::{engine::manager::BattleManagers, test_support::init_config};

mod consume;
mod exclusion;
mod features;
mod setup;
mod stacking;
mod uid;
